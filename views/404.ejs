<% const renderBody = () => { %>
  <section class="form-shell">
    <h2>페이지를 찾을 수 없습니다.</h2>
    <p><%= typeof message !== "undefined" ? message : "잘못된 경로입니다." %></p>
    <a class="btn primary" href="/">메인으로 돌아가기</a>
  </section>
<% } %>

<%- include("layout", {
  renderBody,
  pageTitle: "404"
}) %>



